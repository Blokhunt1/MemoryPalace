<nav class="floating-navbar">
    <NavLink class="navbar-brand" href="/" Match="NavLinkMatch.All">
        <div class="brand-logo">🏰</div>
        <span>Memory Palace</span>
    </NavLink>
    
    <div class="navbar-nav">
        <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
            <span>🏠</span>
            <span>Home</span>
        </NavLink>
        <AuthorizeView>
            <Authorized>
                <NavLink class="nav-link" href="upload">
                    <span>📤</span>
                    <span>Create Palace</span>
                </NavLink>
                <NavLink class="nav-link" href="palaces">
                    <span>🏰</span>
                    <span>My Palaces</span>
                </NavLink>
            </Authorized>
        </AuthorizeView>
    </div>
    
    <div class="navbar-actions">
        <AuthorizeView>
            <Authorized>
                <span class="text-muted small">Welcome, @context.User?.Identity?.Name</span>
                <a class="nav-link" href="/logout">
                    <span>🚪</span>
                    <span>Logout</span>
                </a>
            </Authorized>
            <NotAuthorized>
                <a class="nav-link" href="/login">
                    <span>🔒</span>
                    <span>Login</span>
                </a>
            </NotAuthorized>
        </AuthorizeView>
    </div>
</nav>